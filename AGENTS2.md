# BBot_velocity_jump 项目协作规范

## 适用范围

本文件适用于 `/home/admin/bbot_ws_new` 及其子目录中的 BBot ROS 2 控制器、仿真、日志和绘图代码。

## 修改原则

- 修改前先检查工作区状态，保留用户已有的未提交改动。
- 不得使用破坏性 Git 操作覆盖或回退用户代码，包括 `git reset --hard`、`git checkout -- <file>`、强制覆盖未提交修改等。
- 原有 `bbot_jump_controller.cpp` 是已验证控制器；除非用户明确要求，不修改其行为。
- `bbot_velocity_jump_controller.cpp` 是目标速度跳跃控制器，顶层跳跃状态机必须保持：

  `BALANCE → SQUAT → THRUST → FLIGHT → TOUCHDOWN_BUFFER → RECOVERY`

- `TOUCHDOWN_BUFFER` 内部允许使用子阶段，但不得破坏顶层状态机。当前推荐内部流程为：

  `CATCH → PREPARE_BRAKE → BRAKE → POST_BRAKE_HOLD`

- 控制器切换必须保持 Position/Effort 互斥；切换失败时不得停留在无有效腿部控制的状态。
- 涉及力矩的改动必须同时考虑：幅值限幅、变化率限幅、姿态保护、异常超时和状态切换连续性。
- 修改前必须先读当前实际代码和最新日志，不允许凭旧版本变量名、旧参数或记忆直接改。
- 参数试调值必须明确标注为“试验/调参值”，不得表述为理论唯一值。
- 未实际运行 Gazebo/ROS 2 时，不得宣称运行时验证成功；改完代码后由用户自行运行验证。

## BBot 控制约定

- 四腿关节 Effort 顺序：左髋、左膝、右髋、右膝。
- 当前模型面向约 22 kg BBot、轮半径约 `0.07 m`、ROS 2 Iron / Gazebo 环境。
- IMU 当前符号约定：`pitch > 0` 表示前倾，`pitch < 0` 表示后仰。
- 当前轮速运动学约定：

  ```cpp
  x_dot_ = -wheel_radius_ * 0.5 * (left_wheel_vel_ + right_wheel_vel_);
  ```

  因此所有恢复/停车控制必须先按日志验证命令与 `x_dot_` 的真实符号关系，不能直接套用旧 LQR 的 `k_x_dot * x_dot_`。
- 默认跳跃目标为相对高度 `0.20 m`；目标离地速度默认按

  `sqrt(2 * g * jump_height)`

  计算，除非用户通过参数覆盖。
- 不得把腿部正运动学高度 `current_z_` 或 `current_z_dot_` 单独当作轮子世界系离地/上升/下降证明；离地判别必须结合上升速度、IMU 卸载、接触/力矩信息和连续确认。
- 收腿/展腿轨迹应提供连续的位置、速度和必要时加速度参考；不得每周期用当前高度重新生成会造成目标跳变的轨迹。
- 保护路径优先于达到跳高目标：姿态、力矩、腿长反向压缩、异常接触或超时触发时，进入既有保护/恢复逻辑。

## 跳跃后仰抑制与状态机技术规范

### 1. THRUST：推地姿态门控与力矩约束

- 推地前与推地中执行姿态门控：要求 `|pitch - balance_offset| <= 0.06 rad` 且 `|pitch_rate| <= 0.45 rad/s` 连续稳定约 30 ms。
- 出现后仰趋势时（典型判据：`pitch_err < -0.08 rad` 或 `pitch_rate < -0.80 rad/s`）：
  - 暂停伸腿轨迹推进；
  - 额外推力降为零；
  - 保留重力支撑、低增益构型保持和受限轮速姿态修正；
  - 连续阻塞超过约 `0.20 s` 或整体超时则进入恢复路径。
- 推地逆解高度保护：`target_ik_z = std::max(des_z, current_z_)`，避免后仰时目标腿长反向缩短。
- 后仰明显时禁止髋关节输出会进一步拉倒机身的反向大力矩。
- 严控关节期望速度数值微分，避免高频交替抖振。
- 离地切换条件应至少包括其一：
  - 目标上升速度连续达标；
  - 失重/法向载荷卸载连续确认；
  - 推地轨迹结束且实际仍高速上升。
- 离地时推地前馈力矩必须平滑卸载到零，不能单周期截断。

### 2. FLIGHT：腾空子阶段与角动量控制

#### ATTITUDE_ARREST

- 离地后优先抑制俯仰角速度，不急于收腿。
- 保持离地瞬时腿长附近，腿部位置刚度保持柔顺，避免收腿反作用角动量继续把机身推向后仰。
- 髋关节可施加有限主动俯仰阻尼/回正力矩。
- 若规定时间内姿态未恢复或姿态超限，放弃 TUCK，直接进入 `PROTECTIVE_DEPLOY`。

#### TUCK

- 使用固定时长五次多项式平滑收腿，推荐目标高度约 `L_RETRACT_ = 0.32 m`。
- 收腿期间持续做机身姿态阻尼，不允许腿轨迹独立运行而忽略机身角动量。

#### EXTEND / PROTECTIVE_DEPLOY

- 顶点附近或保护条件触发后开始展腿。
- 展腿必须使用连续轨迹，禁止线性瞬跳或目标速度不连续。
- `L_TOUCH_` 必须以“轮子优先触地、膝关节不先着地、保留有效缓冲行程”为准；修改时以当前模型/日志为依据，不照搬旧常数。

#### 空中轮速

- 默认 `air_wheel_sign = -1.0`。
- 空中轮速命令是速度目标，不是直接电机力矩；下层速度环实际产生电机转矩。
- 下降阶段必须限制空中轮速，避免带大轮速触地。
- 空中姿态控制可使用 P/D，但 D 项不得在机身仍明显偏离目标时过早完全抵消 P 项。

### 3. TOUCHDOWN_BUFFER：触地捕获与制动

顶层仍为 `TOUCHDOWN_BUFFER`，内部推荐使用：

`CATCH → PREPARE_BRAKE → BRAKE → POST_BRAKE_HOLD`

#### CATCH

- 目标：触地第一时间把支撑点移动到倾倒机身下方，先“接住机身”，不优先停车或回原位置。
- 保留 FLIGHT 最后一帧轮速命令，避免触地瞬间人为清零造成控制断层。
- CATCH 阶段不要加入旧式 `k_x_dot * x_dot_` 全状态 LQR 项；当前实际日志已证明该项在现有符号链上可能形成正反馈。
- CATCH 退出不要只看 `pitch`，应结合相平面/捕获变量：

  ```text
  omega = sqrt(g / h)
  eta   = pitch_err + pitch_rate / omega
  ```

  其中 `h` 只能在接地后作为近似有效高度使用，不能把飞行阶段的腿部几何高度当世界系 COM 高度。
- 当机身虽然仍有少量后仰，但 `eta` 已接近可恢复边界且角速度正在回正时，应及时结束强 CATCH，避免继续积累后退速度。

#### PREPARE_BRAKE

- 目标：在不继续大量积累水平速度的情况下，建立少量前倾/正 capture 储备，为刹车做准备。
- 不允许像 CATCH 一样持续把轮速推到饱和。
- `PREPARE → BRAKE` 应要求真实姿态已接近平衡侧，不应仅因为瞬时正角速度就过早进入 BRAKE。

#### BRAKE

- 目标：逐步把后退轮速参考斜坡降到 0，而不是瞬间反向。
- 轮速参考应显式维护 `brake_ref`，并用实测 `x_dot_` 做速度误差闭环。
- `brake_ref` 尚未降到 0 时，姿态项不得重新把轮速目标长期拉大，破坏停车目标。
- BRAKE 减速过程中若 `capture` 已接近后方边界且 `pitch_rate` 仍为负，应暂停降低 `brake_ref`，先恢复少量支撑点后退速度；若 `capture` 继续明确恶化，应在低轮速阶段提前回到 CATCH，不能等大角度后倒才止损。
- `BRAKE → POST_BRAKE_HOLD` 除了要求 `brake_ref` 接近零，还必须确认后方 `capture` 裕量和角速度安全，禁止带明显负 `capture` 进入低权限 HOLD。
- BRAKE 阶段禁止“回起跳位置”或用旧 `target_x_` 做位置回中；落地后退了多少先接受多少，优先站住。
- 前倾保护必须足够早，但不能在轻微前倾时突然给大反向轮速导致过冲。

#### POST_BRAKE_HOLD

- 当 `brake_ref < 0.06` 后，不应继续用弱 BRAKE 姿态增益长期站立。
- 进入 `POST_BRAKE_HOLD`，目标改为优先把机身拉回 `balance_offset_`：
  - 姿态明显偏离时，只优先救姿态；
  - 全程保留轮速阻尼，但姿态明显偏离时可以降低其权重，不能把速度阻尼突然撤为零；
  - 不加入旧位置回中项。
- HOLD 的轮速权限和变化率必须明显低于 CATCH。后退速度已经形成时，应随 `|x_dot|` 增大动态收紧继续后退的正命令上限，防止姿态环再次把轮速推到饱和。
- `brake_ref` 已归零后禁止仅因普通姿态波动重新进入强 CATCH；只有重新建立了明确的高速失稳判据且轮速仍在可控范围时，才允许重新捕获。
- HOLD 是 BRAKE 与 RECOVERY 之间的短暂姿态接管阶段，不新增顶层状态。

### 4. 触地腿部力矩与垂直阻抗

- 触地缓冲阶段不要突然叠加大幅髋部 `Jz^T F` 垂向支撑力矩；理想 IK 角与实际落地角存在偏差时，也不要突然启用高刚度髋 PD。
- 髋部优先承担有限姿态力矩，膝关节承担主要垂向阻抗；若当前版本采用混合承重，必须保证从 TOUCHDOWN 到 RECOVERY 的支撑分配连续，禁止状态切换时瞬间重新分配整条腿负载。
- 当前默认单腿缓冲参数：
  - `K_Z_BUFFER_ = 450 N/m`
  - `D_Z_BUFFER_ = 75 N·s/m`
  - `F_Z_BUFFER_MAX_ = 240 N`
- 更换质量、轮胎、接触模型或 Gazebo 参数后必须重新检查触地压缩速度、反弹和力矩峰值。
- 力矩命令必须有幅值限幅与斜率限幅；注释中的物理限值与代码实际 clamp 数值必须一致。

### 5. TOUCHDOWN_BUFFER → RECOVERY 退出条件

- 禁止仅按固定时间或仅按 `current_z_dot_` 切换到 RECOVERY。
- 必须同时满足：
  - `brake_ref` 已基本归零；
  - 腿长/垂直速度稳定；
  - `pitch` / `pitch_rate` 进入稳定域；
  - `x_dot_` 进入稳定域；
  - 连续稳定计数满足要求。
- 当前实现推荐连续稳定 `20` 个 200 Hz 周期，即约 `100 ms`。
- 推荐稳定域作为起始调参值：

  ```text
  |current_z_dot_| < 0.05 m/s
  |pitch_err|      < 0.06 rad
  |pitch_rate|     < 0.20 rad/s
  |capture/eta|    < 0.03 rad
  |x_dot_|         < 0.12 m/s
  |cmd_x|          < 0.10 m/s
  brake_ref        < 0.06 m/s
  ```

- 这些是当前项目调试门槛，不是理论固定常数；修改时必须结合最新日志。

### 6. RECOVERY：软接管与稳态恢复

- RECOVERY 的首要目标是“静稳接管”，不是第二次强 CATCH。
- 切入 RECOVERY 时锁存当前 `x_` 作为新的位置参考，禁止强行回到起跳前位置。
- 低位保持阶段使用较低轮速增益、较小轮速限幅和较低变化率，避免刚进入 RECOVERY 就再次大幅运动。
- `post_landing_pitch_ref_` 必须严格回到物理平衡偏置 `balance_offset_`，禁止人为虚增前倾目标角。
- RECOVERY 初期腿部关节 PD 与 TOUCHDOWN 尽量连续；开始站高后再逐渐恢复正常增益。
- `TOUCHDOWN_BUFFER → RECOVERY` 时必须继承 TOUCHDOWN 最后一帧的期望腿长/关节构型，不能把受载后的实测腿长当作新的期望腿长，否则会在状态边界产生关节目标和力矩阶跃。
- RECOVERY 低位保持与开始站高时应连续继承 `POST_BRAKE_HOLD` 的轮速控制律；若确需改变增益、阻尼或限幅，必须显式做平滑插值，禁止在状态边界一次性切换。
- RECOVERY 中的关节力矩限幅和斜率限制应比 THRUST 更保守。
- 只有低位姿态、轮速、垂直速度连续稳定后，才允许五次轨迹抬升到 `L_STAND_`。
- 当前速度跳跃版沿用旧版的保守策略：恢复完成后默认继续留在 `RECOVERY`，
  不自动切回 `BALANCE`。只有后续日志证明两套控制律的支撑力、关节目标、
  轮速目标在边界上均连续时，才允许恢复自动切换。

## 日志和绘图

- 跳跃日志目录：`src/bbot_balance_controller/src/data_logs/`。
- 新增 CSV 字段必须追加到末尾，避免破坏旧日志读取。
- 绘图脚本必须兼容旧 CSV 缺字段情况。
- `hip_cmd_*` / `knee_cmd_*` 表示控制器发出的关节力矩命令；`*_effort_*` 表示 `JointState` 中的测量值，严禁混用。
- 每次跳跃只在真正结束（恢复完成或明确失败）后写一条汇总记录。
- 修改日志字段后，至少用一份现有日志验证 `plot_jump_log.py` 兼容性。
- 调试 TOUCHDOWN 时日志至少应包含：
  - 当前子阶段 `CATCH/PREPARE/BRAKE/HOLD`；
  - `pitch`、`pitch_err`、`pitch_rate`；
  - `capture/eta`；
  - `x_dot`、`xerr`；
  - `brake_ref/aux`；
  - `cmd_target`、实际 `cmd`；
  - 必要时 P/D/guard 分量。
- 调试 RECOVERY 时日志至少应包含：
  - `z`、`z_dot`；
  - `pitch`、`pitch_rate`；
  - `x_dot`；
  - `cmd_target`、`cmd`；
  - 单腿支撑力；
  - 四个关节命令力矩。

## 构建与验证

仓库根目录：

```bash
source /opt/ros/iron/setup.bash
colcon build --packages-select bbot_balance_controller --symlink-install
```

Python 绘图/优化脚本修改后：

```bash
python3 -m py_compile src/bbot_balance_controller/src/data_logs/plot_jump_log.py
```

提交前至少检查：

- C++ ROS 2 包可编译；
- Python 脚本可解析；
- `git diff --check` 无空白错误；
- CSV 表头与每行字段数一致；
- 未修改超出用户请求范围的控制器、模型或日志；
- 未覆盖用户已有未提交改动；
- 未声称未经实际 Gazebo 测试的运行结果。

## 仿真验收重点

- THRUST 推力和四个关节指令无明显单周期跳变。
- 离地时目标验收：`|pitch| < 0.15 rad` 且 `|pitch_rate| < 1.0 rad/s`，离地推力前馈已平滑卸载。
- FLIGHT 姿态刹车有效，不发生空中二次后仰翻滚。
- 正常进入 FLIGHT 时记录离地确认计数、实际离地速度和姿态条件。
- 轮子应先于膝关节接地，且落地前机身姿态处于可捕获范围。
- TOUCHDOWN 应能依次完成 `CATCH → PREPARE → BRAKE → HOLD`，且不长时间停留在任一临时子阶段。
- `brake_ref → 0` 后，HOLD 应把 `pitch → balance_offset_`，而不是继续用弱 BRAKE 控制长期站立。
- 连续跳跃测试应确认无翻倒，至少 4/5 完成 `TOUCHDOWN_BUFFER → RECOVERY`，并保留控制日志和汇总 CSV。
- 若出现 `Controller already loaded`、初始 `pitch≈±π`、`x_dot≈±2.1 m/s` 等异常，应先视为旧 `/controller_manager` / DDS / Gazebo 状态污染，不把该次结果用于评价控制器。
- 用户负责实际运行 Gazebo；助手只负责代码修改、静态检查、构建建议和日志分析，除非当前环境确实完成了运行时验证。

## 2026-09-03~09-04 跳跃失稳问题记录

- 早期问题：下蹲结束仍有明显下沉速度，导致推地实际离地速度不足；增加沉降稳定门控后，干净仿真中离地速度提升到约 `1.5~1.7 m/s`。
- 早期落地问题：轮子能先着地，但机身触地时已明显后仰；仅靠普通 LQR/PID 无法及时捕获。
- 已验证 CATCH 方向正确：触地后使用轮子后退追质心可以显著降低后仰角速度。
- 单纯延长/加强 CATCH 会导致轮子后退速度持续积累并饱和，因此必须在相平面进入可恢复域后及时切到 PREPARE/BRAKE。
- 纯角度阈值不适合作为 CATCH 退出条件；采用 `eta = pitch_err + pitch_rate / sqrt(g/h)` 的相平面指标更能识别“角度仍后仰但已经正在回正”的窗口。
- PREPARE 的作用不是停车，而是建立少量前倾/正 capture 储备；过短会导致 BRAKE 一开始就重新后倒，过长会导致前倾过冲。
- BRAKE 必须显式维护后退轮速参考并逐步降到 0；不能让姿态项在 `brake_ref=0` 后重新把轮速目标拉回大正值。
- BRAKE 内的位置回中曾造成持续小轮速偏置并慢慢把机身推向前倒，因此已确定：落地后先接受实际落地点，不在 BRAKE/HOLD 中回起跳位置。
- `brake_ref=0` 后继续使用弱 BRAKE 姿态控制会造成“视觉上已停住，但机身从约 0.15 rad 前倾继续发散”。因此加入 `POST_BRAKE_HOLD`，用更强姿态控制把机身拉回 `balance_offset_` 后再进入 RECOVERY。
- V13 日志进一步证明：HOLD 中“姿态误差超过阈值就完全关闭速度阻尼”会造成二次后退发散；机器人曾在 `pitch≈0.059 rad、x_dot≈-0.083 m/s` 基本捕获后，又把 `x_dot` 累积到约 `-1.9 m/s` 并重新进入 CATCH。HOLD 必须始终保留速度阻尼、降低指令斜率，并按后退速度收紧继续后退权限。
- V14 日志证明 TOUCHDOWN 本身已完成捕获，但 `TOUCHDOWN_BUFFER → RECOVERY` 仍存在双重控制阶跃：关节目标从约 `(-0.163, 0.196)` 跳到 `(-0.039, 0.115) rad`，首帧髋/膝命令从约 `(-1.48, -23.75)` 跳到 `(+1.69, -27.75) N·m`；轮速控制也从 HOLD 的 `cmd≈-0.002` 切成 RECOVERY 的 `cmd≈+0.018`，之后逐步饱和到 `+0.8 m/s` 并把 `x_dot` 拉到约 `-0.8 m/s`。因此恢复阶段必须同时保持腿部期望构型、支撑力和轮速律连续，并从上一阶段的期望腿长而非受载实测腿长启动站高轨迹。
- V15 日志确认 V14 的腿部连续性修复有效：状态边界前后髋/膝命令均保持在约 `(-1.4, -23.5) N·m`，支撑力约 `190 N`。剩余失稳来自 HOLD 的“等速死锁”：切换时 `pitch≈0.010 rad、gyro≈-0.154 rad/s、capture≈-0.050、x_dot≈-0.074 m/s、cmd≈+0.153 m/s`，并非真正静稳；随后旧动态上限在 `x_dot≈-0.455 m/s` 时恰好把正命令限制为 `+0.455 m/s`，轮子不再产生追赶质心所需的后向加速度而机身继续后倒。HOLD/RECOVERY 的稳定门槛必须同时检查 `capture` 和实际 `cmd`；后倒 capture 明确越界时只开放受限的微捕获速度余量，不能形成“命令目标等于当前速度”的锁死状态。
- V16 日志没有进入 RECOVERY，真正的首个失稳点提前到 BRAKE 后半段：`capture` 从 `+0.024` 下降到 `-0.039`、再到 `-0.122` 时，代码仍持续把 `brake_ref` 从约 `0.43` 降到 `0.14 m/s`；最终以 `pitch≈-0.05 rad、gyro≈-0.85 rad/s、capture≈-0.249` 进入 HOLD，微捕获启动时已错过低速窗口。BRAKE 必须在相平面裕量转坏时暂停减速/恢复小幅速度参考，并在 `capture<-0.09 rad` 且仍后转时提前回到 CATCH。
- V17 日志暴露出与 V15 完全镜像的前倒死锁：机器人在约 `6.10~7.50 s` 看似短暂稳定，但 `pitch` 从约 `0.199` 增到 `0.257 rad`、`x_dot` 从约 `-0.062` 积累到 `+0.736 m/s`，轮速指令逐渐卡到 `-0.85 m/s`；之后 `x_dot≈+0.85 m/s`、`cmd≈-0.85 m/s`，轮子没有剩余加速度追到前倾质心下方。POST_BRAKE_HOLD/RECOVERY 的 capture 微捕获和动态限幅必须前后对称：后倒时正命令应略大于当前后退速度，前倒时负命令幅值也应略大于当前前进速度。
- 旧版 `bbot_jump_controller.cpp` 的可迁移稳定机制是：轮毂控制前后对称、恢复完成后持续留在同一 `RECOVERY` 控制律。旧版的人为前倾参考 `0.085 rad` 和未经当前符号链验证的完整平移 LQR 不应直接照搬。
- V18 日志显示上述 V16 后倒保护又产生了慢性正反馈：触地后 `z≈0.365 m` 基本稳定，但 `x` 在约 7 s 内从 `+0.09 m` 漂到 `-6.61 m`；`cmd` 与 `x_dot` 反向等幅增长，说明 `brake_ref=max(brake_ref,-x_dot+0.08)` 在俯仰小振荡中不断抬高停车参考。BRAKE 中 `brake_ref` 必须单调下降，姿态裕量变差时只能短暂停止衰减，不能恢复到当前后退速度；低速阶段再用小幅落地点位置回中消除累计漂移。
- V19 修复要求：HOLD/RECOVERY 的 capture 只能放宽对应方向的指令饱和上限，不能把轮速目标强制设为“当前同向速度+余量”。低速稳定时必须同时使用姿态反馈、速度阻尼和实际落地点位置反馈，并对位置/速度设置死区；否则会从后退等速死锁切换成前进等速死锁。实际落地点在 `PREPARE→BRAKE` 时锁存，作为跳后最终位置参考。
- V20 已落实 V19：HOLD/RECOVERY 采用带 `0.02 m` 位置死区、`0.03 m/s` 速度死区的姿态+速度+位置闭环；capture 仅把单侧限幅从 `0.75` 放宽至 `1.10 m/s`，不再强制追赶当前速度；BRAKE 低速段加入小幅落地点回中；新增 `touchdown_phase/brake_ref/capture_state/target_x/x_error/cmd_target` CSV 诊断字段。新增字段只追加在末尾，旧字段顺序保持不变。
- RECOVERY 曾因固定/宽松超时提前接管，触发轮毂和腿部控制律同时变化导致关节力矩发散；现在必须等 BRAKE/HOLD 真正稳定后再切换。
- 恢复阶段曾出现 `cmd_x=-3 m/s`、轮速约 `-30 rad/s`、`x_dot=+2.1 m/s` 的持续滑行，已确认旧式 `k_x_dot` 符号链可能形成正反馈。任何恢复停车环都必须用实测 `x_dot_` 做符号回归验证。
- 直接把大幅髋部 `Jz^T F` 加回触地/恢复会造成俯仰冲量；完全不管理构型又会出现腿侧折。必须区分机身俯仰控制、垂直支撑和腿部构型保持三个职责。

## 协作输出要求

- 用户说“帮我改”时，优先基于最新实际源文件修改并返回完整可替换文件；不要只给抽象伪代码。
- 如果当前最新源文件不明确，先读取用户最新上传文件或要求用户上传，不从旧版本猜。
- 修改后说明：改了哪些位置、为什么、哪些数值属于试调参数、是否实际编译/运行。
- 如生成完整源码，同时可附 diff/patch 方便审核。
- 对日志结论必须引用具体时间点、状态、`pitch/pitch_rate/x_dot/cmd` 等数值，不做无日志支撑的方向判断。
